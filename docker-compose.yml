services:
  db:
    image: postgres:15
    container_name: zeus-db
    environment:
      POSTGRES_DB: zeus-local-db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - 5412:5432
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d zeus-local-db"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - my-network

volumes:
  postgres_data:

networks:
  my-network:
    driver: bridge
